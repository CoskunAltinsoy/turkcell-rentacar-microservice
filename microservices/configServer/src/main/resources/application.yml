spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        default-label: main
        git:
          uri: https://github.com/CoskunAltinsoy/config-server
server:
  port: 8888